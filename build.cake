#load ".build\parameters.cake"
#load ".build\tasks.cake"

//////////////////////////////////////////////////////////////////////
// PARAMETERS
//////////////////////////////////////////////////////////////////////

const string solution = "nullable-reference-types.sln";
const string octoProject = "";
const string octoPublishProject = "";

isDotNetCore = true;

//////////////////////////////////////////////////////////////////////
// CUSTOM TASKS
// Add tasks as required, optional
//////////////////////////////////////////////////////////////////////

Task("Install-DotNetCoreSdk-3.0")
    .WithCriteria(() => isDotNetCore)
    .Does(() =>
{
    InstallDotnetSdk("3.0.100");
});

//////////////////////////////////////////////////////////////////////
// TASK TARGETS
// Change the default as required
// Optionally add more
//////////////////////////////////////////////////////////////////////

Task("Default")
    .IsDependentOn("Info")
    .IsDependentOn("Install-DotNetCoreSdk-3.0")
    .IsDependentOn("Build");

//////////////////////////////////////////////////////////////////////
// EXECUTION
//////////////////////////////////////////////////////////////////////

RunTarget(target);
